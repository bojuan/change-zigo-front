import { Button } from "@/components/ui/button";
import Layout from "@/app/layout";

export default function Home() {
  return (
    <Layout>
      <div>
        <Button>Init</Button>
      </div>
    </Layout>
  );
}
